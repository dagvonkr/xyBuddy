angular.module('xyBuddy', ['lvl.directives.dragdrop'])
.controller('mainCtrl', function($scope){

	// Her må jeg lage DD logikken. 1. dra, 2. endre ny plassering, 3. hva skjer hvis man overlapper
	// Det blir å dra inn i slots i første omgang
	
	// Dra inn element
	$scope.dropped = function(dargElement, dropElement) {
		
	}

})